# MongoDB connection string (e.g. from MongoDB Atlas)
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/paytm

# Secret used to sign JWTs — generate one with: openssl rand -hex 32
JWT_SECRET=

# Gmail credentials used by nodemailer for OTP emails
AUTH_EMAIL=
AUTH_PASS=
